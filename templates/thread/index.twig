<!DOCTYPE html>
<html>
    <head>
	<meta charset="utf-8">
	<meta property="og:image" content="/img/board.jpg" />
	<meta name="keywords" content="FiNC, 掲示板, PHP, 練習" />
	<meta name="description" content="研修1修了テストの課題" />
	<meta property="og:description" content="研修1修了テストの課題" />
	<title>{{ thread.thread_name }} | 掲示板だよ</title>
    </head>

    <body>
	{{ user_id }}

	{% for reply in replies %}
	    {% for posted in posted_user_names %}
		{% if reply.user_id == posted.user_id %}
		    <li>メッセージ：{{ reply.message }} ユーザー：{{ posted.user_name }}</li>
		{% endif %}
		{% if user_id == posted.user_id %}
		    <button>編集</button>
		{% endif %}
	    {% endfor %}
	{% endfor %}
	<a href='/reply/list'>投稿したメッセージ一覧</a>
	<a href='/user/logout'>ログアウト</a>
	<a href='/thread/list'>掲示板一覧</a>
    </body>
</html>
